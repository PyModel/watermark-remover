# Deny by default: only what is explicitly allowed below enters a build
# context. Images copy skills/ and pyproject.toml only; everything else
# (tests, research reference checkout, venvs, tooling) stays out.
*
!skills/
!skills/**
!pyproject.toml
